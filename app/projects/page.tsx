import type { Metadata } from 'next'
import Image from 'next/image'
import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { Footer, Navbar, SectionLabel, WhatsAppButton } from '@/components/site-shell'
import { projects } from '@/lib/site-data'
export const metadata: Metadata = { title: 'Solar Projects in Maharashtra', description: 'Explore residential, commercial and industrial solar projects by Surya Urja.' }
export default function Page(){ return <><main><section className="bg-primary pb-16 pt-32 text-white"><Navbar/><div className="container-site"><SectionLabel>Real roofs. Real results.</SectionLabel><h1 className="font-display text-5xl font-semibold tracking-tight sm:text-6xl">Work that speaks for itself.</h1><p className="mt-5 max-w-xl text-white/70">A look at the homes, businesses and facilities we&apos;ve helped make more independent.</p></div></section><section className="section-pad"><div className="container-site grid gap-6 md:grid-cols-3">{projects.map(project=><article key={project.slug} className="group overflow-hidden rounded-3xl bg-white shadow-sm"><div className="relative aspect-[4/3]"><Image src={project.image} alt={`${project.name} solar project`} fill className="object-cover transition duration-500 group-hover:scale-105"/></div><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">{project.type} · {project.location}</p><h2 className="mt-3 font-display text-xl font-semibold">{project.name}</h2><div className="mt-5 flex items-center justify-between"><span className="text-sm text-muted-foreground">{project.capacity} system</span><Link href={`/projects/${project.slug}`} className="text-primary"><ArrowRight size={18}/><span className="sr-only">View {project.name}</span></Link></div></div></article>)}</div></section></main><Footer/><WhatsAppButton/></> }
