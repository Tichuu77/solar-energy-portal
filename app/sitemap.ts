import type { MetadataRoute } from 'next'
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL || 'https://surya-urja.example'; return ['', '/about','/solutions/residential','/solutions/commercial','/solutions/industrial','/residential-solar','/commercial-solar','/industrial-solar','/projects','/solar-calculator','/contact','/blog','/privacy-policy','/terms-and-conditions'].map(path=>({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: 'monthly', priority: path===''?1:.7 })) }
